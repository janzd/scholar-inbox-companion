{
  "name": "scholar-clipper",
  "version": "0.1.1",
  "private": true,
  "description": "A personal Chrome extension for saving arXiv papers to Scholar Inbox collections.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check extension/core.js && node --check extension/background.js && node --check extension/popup.js && node --check extension/benchmark.js",
    "test": "node --test tests/*.test.js"
  }
}
