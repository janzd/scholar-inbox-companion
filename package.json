{
  "name": "scholar-inbox-companion",
  "version": "0.2.0",
  "private": true,
  "description": "Save papers from research websites and PDFs to Scholar Inbox collections.",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "check": "node --check extension/core.js && node --check extension/sources.js && node --check extension/pdf.js && node --check extension/background.js && node --check extension/popup.js && node --check extension/benchmark.js",
    "test": "node --test tests/*.test.js",
    "vendor:pdf": "node scripts/vendor-pdf.mjs"
  },
  "devDependencies": {
    "linkedom": "0.18.13",
    "pdfjs-dist": "6.3.289"
  }
}
